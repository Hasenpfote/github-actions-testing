name: _check

on:
  issues:
    types: [opened, closed]
  pull_request_target:
    types: [opened, closed]

jobs:
  check:
    permissions:
      issues: write
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - name: Debug
        shell: bash
        run: |
          echo "${{ github.actor }}"
          echo "${{ github.triggering_actor }}"
