name: 🚧🧰 Maint - Dump rate limit

on:
  workflow_dispatch:

jobs:
  main:
    #permissions:
    #  actions: read

    runs-on: ubuntu-latest
    steps:
      - name: Dump rate limit 
        uses: actions/github-script@v6
        with:
          script: |
            const rate_limit = await github.request('GET /rate_limit', {});
            console.info(rate_limit);

      - name: Debug
        shell: bash
        run: |
          echo "::notice file={name},line={line},endLine={endLine},title={title}::{message}"
