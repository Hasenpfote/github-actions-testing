[tox]
envlist =
    py37
    py38
    black
    isort
skipsdist = true
isolated_build = true

[testenv]
allowlist_externals =
    poetry
commands =
    poetry install -v
    poetry run pytest -v

[testenv:black]
deps = black
commands =
    poetry run black . --check --diff --color

[testenv:isort]
deps =
    isort
    colorama
commands =
    poetry run isort . --check --diff --color
